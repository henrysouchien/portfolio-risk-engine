# Core Data & Analysis
pandas>=3.0.0
numpy>=2.4.0
plotly>=5.21.0
statsmodels>=0.14.0
pyarrow>=23.0.0  # Required for parquet operations in portfolio_risk_engine/data_loader.py and fmp/cache.py

# Web Framework & API (FastAPI-based)
fastapi>=0.135.0
uvicorn[standard]>=0.41.0
pydantic>=2.10.0
starlette>=0.52.0  # SessionMiddleware used in app.py

# Rate Limiting & Middleware
slowapi>=0.1.9

# Database & Connection Pooling
psycopg2-binary>=2.9.5

# Caching & Performance
cachetools>=7.0.0
redis>=7.3.0
celery>=5.3.0,<6.0

# Data Fetching & HTTP
requests>=2.32.0
httpx>=0.28.0  # Async HTTP client for gateway proxy (routes/gateway_proxy.py)

# Configuration & Environment
python-dotenv>=1.0.0
pyyaml>=6.0

# Authentication & Security
google-auth>=2.48.0  # google.oauth2.id_token + google.auth.transport in services/auth_service.py
certifi>=2026.0.0  # SSL fix for IBKR Flex (ibkr/flex.py), also used by providers/plaid_loader.py
# NOTE: cryptography should be >=46.0.5 (CVE-2024-12797 + CVE-2026-26007), but
# snaptrade-python-sdk 11.0.169 pins cryptography<44.0.0 — blocking the fix.
# Installed version is 46.0.5 (works fine, SnapTrade's cap is overly conservative).
# On fresh installs from requirements.lock: pip install cryptography>=46.0.5 after.
# TODO: remove this comment when SnapTrade loosens their upper bound.
cryptography==46.0.5

# AI & Language Models
anthropic>=0.84.0
openai>=2.26.0
fastmcp==3.2.3  # MCP server for Claude Code integration (mcp_server.py, fmp/server.py, ibkr/server.py)

# Date Utilities
python-dateutil>=2.8.0  # relativedelta used in mcp_tools/tax_harvest.py

# Financial Data Integration
plaid-python>=38.0.0
snaptrade-python-sdk>=11.0.0
ib_async>=2.1.0
nest-asyncio>=1.6.0  # Required for ib_async inside FastMCP event loop (ibkr/market_data.py)
schwab-py==1.5.0

# AWS (Secrets Manager for Plaid/SnapTrade credentials)
boto3>=1.42.0

# Testing Dependencies
pytest>=9.0.0
psutil>=7.0.0  # Used in tests/performance and utils/logging.py
watchdog>=4.0.0  # File watcher for living code map (tools/watch_and_update.py)

# Optimization & Mathematical Computing
cvxpy>=1.8.0
scipy>=1.17.0  # XIRR solver for money-weighted return (core/realized_performance/mwr.py)
